name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  # Frontend Tests
  frontend-tests:
    name: Frontend Build & Tests
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: resuna-web/package-lock.json

      - name: Install dependencies
        working-directory: ./resuna-web
        run: npm ci

      - name: Run TypeScript check
        working-directory: ./resuna-web
        run: npm run typecheck

      - name: Build frontend
        working-directory: ./resuna-web
        run: npm run build

  # Backend Tests
  backend-tests:
    name: Backend Build & Tests
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'maven'

      - name: Build with Maven
        working-directory: ./resuna-web/backend
        run: mvn clean compile

      - name: Run tests
        working-directory: ./resuna-web/backend
        run: mvn test

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: backend-test-results
          path: resuna-web/backend/target/surefire-reports/

  # E2E Tests
  e2e-tests:
    name: E2E Tests (Playwright)
    runs-on: ubuntu-latest
    needs: [frontend-tests]

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: resuna-web/package-lock.json

      - name: Install dependencies
        working-directory: ./resuna-web
        run: npm ci

      - name: Install Playwright Browsers
        working-directory: ./resuna-web
        run: npx playwright install --with-deps chromium firefox

      - name: Run E2E tests
        working-directory: ./resuna-web
        run: npm run test:e2e
        env:
          CI: true

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: resuna-web/playwright-report/
          retention-days: 30

      - name: Upload screenshots
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-screenshots
          path: resuna-web/test-results/

  # Security Audit
  security-audit:
    name: Security Audit
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: resuna-web/package-lock.json

      - name: Run npm audit
        working-directory: ./resuna-web
        run: npm audit --production

      - name: Check for critical vulnerabilities
        working-directory: ./resuna-web
        run: npm audit --audit-level=critical
