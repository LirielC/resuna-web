# Credenciais Firebase Admin (service account) não devem ser commitadas.
# Copie o JSON da consola Firebase para um ficheiro local `firebaseconfig` (ignorado pelo git)
# ou configure o backend com Secret Manager / variável de ambiente conforme a documentação do projeto.
